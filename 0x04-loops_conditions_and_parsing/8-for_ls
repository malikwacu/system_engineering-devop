#!/usr/bin/env bash
# This script lists non-hidden files and prints only the part after the first dash (if any)

for file in *; do
  if [ -f "$file" ]; then
    if [[ "$file" == *-* ]]; then
      echo "${file#*-}"
    else
      echo "$file"
    fi
  fi
done
